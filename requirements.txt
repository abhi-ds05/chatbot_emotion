# === Core NLP & Transformers ===
transformers>=4.40.0
torch>=2.1.0
sentencepiece    # Required by some HF tokenizer models
accelerate       # Optimized device mapping (useful for LLaMA, Mistral)

# === FastAPI for API and Routing ===
fastapi>=0.110.0
uvicorn[standard]>=0.29.0

# === Optional: For Local Testing UI (Gradio or Streamlit) ===
gradio>=4.36.0
# OR
streamlit>=1.35.0

# === Emotion Detection (multimodal support) ===
opencv-python      # For facial emotion recognition
librosa            # For audio-based emotion analysis (if needed)
scikit-learn>=1.3  # Optional for traditional models (fallback or pretraining)
pandas             # For preprocessing and dataset analysis

# === Hugging Face Utility ===
datasets>=2.19.0
huggingface_hub>=0.21.0

# === Logging & Utilities ===
loguru>=0.7.2
pydantic>=2.5.0
python-dotenv      # For loading .env configuration
typer[all]         # CLI UI support

# === Testing ===
pytest>=8.2.0
httpx              # For FastAPI API client testing
